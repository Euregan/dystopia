import { Meta, Canvas } from '@storybook/addon-docs/blocks'
import Loading from './Loading'

<Meta title="Documentation/Content/Loading" component={Loading} />

The Loading component can be dropped anywhere a loading happens.
It's a simple loading pseudo squeleton.

<Canvas>
  <Loading />
</Canvas>

The inside sentence can be customized:

<Canvas>
  <Loading label="Hacking the matrix" />
</Canvas>
