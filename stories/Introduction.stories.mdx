import { Meta, Canvas } from '@storybook/addon-docs/blocks'
import Card from './Card'
import Avatar from './Avatar'
import Flexbox from './Flexbox'
import avatar from './assets/avatar.png'

<Meta title="Introduction" />

# Dystopia

A component library for React, rooted in the past, but powered by the future ✨

<Canvas>
  <Card title="Retro future is the new future" color="#ae1c13">
    <Flexbox direction="row" gap="small">
      <Avatar picture={avatar} size="large" />
      <Flexbox direction="column" gap="small">
        <span>Hello developer</span>
        <span>Welcome to the wonderful land of the Dystopia Design System</span>
        <span>Hope you'll find it to your liking</span>
      </Flexbox>
    </Flexbox>
  </Card>
</Canvas>

## Installation

Installing Dystopia is pretty straightforward, you just need to run the following command:

```
npm install dystopia
```
